Examples for retrio

This folder contains minimal runnable examples demonstrating sync and async
usage of the `retrio` package.

Run the examples from the repository root with:

```bash
python -m examples.sync_example
python -m examples.async_example
```

These examples require no extra dependencies beyond the package itself.
