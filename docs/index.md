# retrio

Retrio provides retry, backoff, and simple resilience primitives with built-in observability hooks.

Quick start

```bash
python -m pip install -e .
```

See the `Usage` and `Examples` pages for API details and runnable samples.
